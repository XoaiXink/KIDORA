@using System.Security.Claims
@{
    ViewData["Title"] = "Hồ sơ quản trị";
    var userName = User.Identity?.Name ?? "Admin";
    var email = User.Claims.FirstOrDefault(c => c.Type == ClaimTypes.Email || c.Type == "email")?.Value ?? "Chưa cập nhật";
    var role = User.Claims.FirstOrDefault(c => c.Type == ClaimTypes.Role || c.Type == "role")?.Value ?? "Administrator";
    var userId = User.Claims.FirstOrDefault(c => c.Type == "UserId")?.Value ?? "N/A";
}

<div class="row">
    <div class="col-lg-4">
        <div class="card shadow-sm">
            <div class="card-body text-center">
                <img src="~/adminassets/images/avatar/1.png" alt="Avatar" class="rounded-circle mb-3" style="width:120px;height:120px;object-fit:cover;">
                <h4 class="mb-1 text-dark">@userName</h4>
                <div class="text-muted">@email</div>
                <span class="badge bg-primary mt-2">@role</span>
            </div>
        </div>
    </div>
    <div class="col-lg-8">
        <div class="card shadow-sm">
            <div class="card-body">
                <h5 class="mb-3 text-dark">Thông tin tài khoản</h5>
                <dl class="row mb-0 text-dark">
                    <dt class="col-sm-4">Mã người dùng</dt>
                    <dd class="col-sm-8">@userId</dd>

                    <dt class="col-sm-4">Họ tên</dt>
                    <dd class="col-sm-8">@userName</dd>

                    <dt class="col-sm-4">Email</dt>
                    <dd class="col-sm-8">@email</dd>

                    <dt class="col-sm-4">Vai trò</dt>
                    <dd class="col-sm-8">@role</dd>
                </dl>
                <div class="alert alert-info mt-3 mb-0 small">
                    Thông tin được lấy từ phiên đăng nhập hiện tại. Kết nối với hệ thống quản trị người dùng để chỉnh sửa chi tiết.
                </div>
            </div>
        </div>
    </div>
</div>


