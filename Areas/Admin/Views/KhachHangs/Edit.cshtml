@model KIDORA.Data.KhachHang

@{
    ViewData["Title"] = "Sửa thông tin khách hàng";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h2 class="mb-4">Sửa thông tin khách hàng</h2>

<div class="card">
    <div class="card-body">
        <form asp-action="Edit" method="post">
            @Html.AntiForgeryToken()

            <!-- Ẩn khoá chính để post lên -->
            <input type="hidden" asp-for="MaKh" />

            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <div class="row">
                <!-- Cột trái: Thông tin tài khoản (từ NGUOI_DUNG) -->
                <div class="col-md-6">
                    <h5 class="mb-3">Thông tin tài khoản</h5>

                    <div class="mb-3">
                        <label class="form-label">Mã khách hàng</label>
                        <input class="form-control" value="@Model.MaKh" disabled />
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Họ tên</label>
                        <input class="form-control"
                               value="@Model.MaKhNavigation?.HoTen"
                               disabled />
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Email</label>
                        <input class="form-control"
                               value="@Model.MaKhNavigation?.Email"
                               disabled />
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Điện thoại</label>
                        <input class="form-control"
                               value="@Model.MaKhNavigation?.DienThoai"
                               disabled />
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Giới tính</label>
                        <input class="form-control"
                               value="@Model.MaKhNavigation?.GioiTinh"
                               disabled />
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Ngày sinh</label>
                        <input class="form-control"
                               value="@(Model.MaKhNavigation?.NgaySinh.HasValue == true
                                                                      ? Model.MaKhNavigation.NgaySinh.Value.ToString("dd/MM/yyyy")
                                                                      : "")"
                               disabled />
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Trạng thái email</label><br />
                        @if (Model.MaKhNavigation != null && Model.MaKhNavigation.EmailConfirmed)
                        {
                            <span class="badge bg-success">Đã xác thực</span>
                        }
                        else
                        {
                            <span class="badge bg-warning text-dark">Chưa xác thực</span>
                        }
                    </div>
                </div>

                <!-- Cột phải: Thông tin thành viên / K-Coin -->
                <div class="col-md-6">
                    <h5 class="mb-3">Thông tin thành viên</h5>

                    <div class="mb-3">
                        <label asp-for="MaHang" class="form-label">Hạng thành viên</label>
                        <select asp-for="MaHang" class="form-select" asp-items="ViewBag.MaHang"></select>
                        <span asp-validation-for="MaHang" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="SoDuKcoin" class="form-label">Số dư K-Coin</label>
                        <input asp-for="SoDuKcoin" class="form-control" />
                        <span asp-validation-for="SoDuKcoin" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="NgayThamGia" class="form-label">Ngày tham gia</label>
                        <input asp-for="NgayThamGia"
                               class="form-control"
                               type="datetime-local"
                               asp-format="{0:yyyy-MM-ddTHH:mm}" />
                        <span asp-validation-for="NgayThamGia" class="text-danger"></span>
                    </div>

                    <p class="text-muted small mt-2">
                        Lưu ý: Việc chỉnh sửa hạng thành viên và số dư K-Coin có thể ảnh hưởng đến quyền lợi của khách hàng.
                    </p>
                </div>
            </div>

            <div class="mt-3 d-flex justify-content-end gap-2">
                <a asp-action="Index" class="btn btn-secondary">
                    Quay lại danh sách
                </a>
                <button type="submit" class="btn btn-primary">
                    Lưu thay đổi
                </button>
            </div>
        </form>
    </div>
</div>
